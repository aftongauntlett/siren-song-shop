# Contact API
RESEND_API_KEY=
CONTACT_FROM_EMAIL=
CONTACT_TO_EMAIL=

# Optional (recommended in production): Upstash Redis for durable rate limiting
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional aliases if using Vercel KV auto-provided names
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional namespace when sharing one Redis DB across projects
RATE_LIMIT_NAMESPACE=siren
